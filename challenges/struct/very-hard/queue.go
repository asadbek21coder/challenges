package veryhard

// Create a Queue struct with methods Enqueue (to add an
// element at the end), Dequeue (to remove and return the
// front element), and IsEmpty (to check if the queue is
// empty). Use a slice as the underlying data structure.

// Queue - Elements []int
// Enqueue() - oxiridan element qohadi
// Dequeue() - boshidan element chopadi
// IsEmpty()  - boshligini tekshiradi
