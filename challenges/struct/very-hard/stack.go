package veryhard

// Create a Stack struct with methods Push (to add an element),
// Pop (to remove and return the top element), and IsEmpty
// (to check if the stack is empty). Use a slice as the
// underlying data structure.

// Stack - Elements []int
// Push()
// Pop()
// IsEmpty()
